services:
  - type: web
    name: nom-035-dashboard
    env: static
    buildCommand: echo "Static site - no build needed"
    startCommand: echo "Static site - no server needed"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
